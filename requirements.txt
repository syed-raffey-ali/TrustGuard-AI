fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
SQLAlchemy==2.*
aiosqlite>=0.20
asyncpg>=0.29
redis>=5
httpx>=0.27
websockets>=13
python-multipart>=0.0.9
pytest>=8
pytest-asyncio>=0.24
openai>=1.50
faster-whisper>=1.0
numpy>=1.26,<3

# Trained taxonomy tier (services/ml) - the instant tier that replaces
# regex-only detection. CPU-only by design; no torch, no GPU required.
scikit-learn>=1.5
scipy>=1.13
joblib>=1.4
# Optional: ONNX export path. char_wb TF-IDF is not currently convertible
# (skl2onnx limitation, verified), so joblib stays authoritative.
onnxruntime>=1.19
skl2onnx>=1.17
